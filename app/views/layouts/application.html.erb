<!DOCTYPE html>
<html>
<head>
  <title>Fbcomments</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
  <script> 
  <%= yield (:javascript) %>
  </script>
</head>
<body>

  <%= fb_connect_async_js %>

<% if current_facebook_user %>
<%= "Hello #{current_user.name}!" #
%>
<%= fb_logout_link("Logout of fb", request.url) %><br />
<% else
# you must explicitly request permissions for facebook user fields.
# here we instruct facebook to ask the user for permission for our website
# to access the user's facebook email and birthday, groups,read_stream
%>
  <%= fb_login_and_redirect('http://127.0.0.1:9292/home/create', :perms => 'email,user_birthday,user_groups,read_stream, publish_stream') %>
<% end %>

<%= yield %>

</body>
</html>
